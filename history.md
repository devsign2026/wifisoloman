# 작업 히스토리

이 프로젝트 진행 상황을 시간 순으로 기록. 새로 진행되는 대로 아래에 계속 추가.

---

## 2026-07-13

### 1. 하드웨어 세팅 완료
라즈베리파이 + 와이파이 공유기로 "CSI"라는 전파 신호를 잡을 수 있게 준비 끝남.
테스트로 패킷 1000개를 잡아봤고, 정상적으로 잘 잡혔음 (`csi_test.pcap`).

> CSI가 뭐냐면: 와이파이 전파가 사람 몸에 부딪히면서 미세하게 흔들리는 걸 숫자로 기록한 것.
> 이 흔들림 패턴을 분석하면 "숨쉬는 것"까지 감지할 수 있음 (레이더처럼).

### 2. 파이에서 노트북으로 파일 가져오는 길 뚫음
- 처음엔 비밀번호를 물어봐서 자동으로 못 가져왔음
- SSH 키 등록(비번 없이 접속되게 하는 방법)해서 해결
- 파이 접속 정보는 `pi_access.md` / `config.sh`에 저장해둠

### 3. "잡은 신호 → 호흡수 숫자" 로 바꿔주는 프로그램 작성
파일: `csi_pipeline.py`

이 프로그램이 하는 일을 쉽게 풀면:
1. 캡처 파일(.pcap) 열기
2. 신호 세기 뽑아내기
3. 잡음 많은 부분 빼고, 진짜 신호 있는 부분만 골라내기
4. 초당 1~2번 정도로 천천히 떨리는 성분만 남기기 (호흡은 느리게 떨리니까)
5. "가장 강하게 반복되는 떨림 주기"를 찾아서 분당 호흡수(BPM)로 환산

### 4. 프로그램 오류 하나 발견 & 수정
신호 세기를 계산하는 과정에서 일부 값이 "0"으로 나오는 지점(전파가 원래 안 잡히는 채널)이 있었는데,
프로그램이 그 지점을 "변화가 제일 크다"고 착각해서 엉뚱한 곳을 보고 있었음.
→ 그 지점을 제외하도록 고쳐서 정상 작동 확인.

### 5. 테스트 실행 결과
`csi_test.pcap`(설치 확인용, 실제 호흡 아님)을 넣고 돌려봄:
- 에러 없이 끝까지 실행됨 ✅
- 단, 진짜 사람이 숨쉰 데이터가 아니라서 나온 숫자(BPM)는 의미 없음. "프로그램이 안 깨지고 잘 도는지" 확인한 것뿐.
- (플롯은 이후 필터 버그가 밝혀져 삭제. 8-25 항목 참조)

### 6. 첫 실측 캡처 (분석은 이후 전부 무효화됨 ⚠️)

SSH로 파이에 원격 접속해 4개 캡처. 전부 패킷 드랍 0개로 정상 캡처됨.
- `baseline_occupied_20260713.pcap`(당시 이름 `baseline_empty.pcap`) 3000패킷 / `breath_12bpm` `breath_15bpm` `breath_20bpm` 각 6000패킷

당시 `csi_pipeline.py`로 분석해 12→17.2, 15→17.2, 20→6.2 BPM을 얻고 "실패"로 판정했으나,
**이때의 분석 결과와 원인 추정(AGC·자세 변화·호흡 실수 등)은 전부 근거를 잃음.** 이유는 두 가지:

1. **밴드패스 필터가 고장나 있었음** → 2026-08-25 11번 항목
2. **`baseline_empty.pcap`(현 `baseline_occupied_20260713.pcap`)이 실제로는 빈 방이 아니었음** → 2026-08-25 10번 항목
   (사람이 있는 방을 노이즈 플로어로 놓고 비교한 것)

수정 후 재분석 결과는 12번 항목 참조.

### 7. 재부팅 관련 확인
- 파이, 맥북 둘 다 **재부팅해도 안전함** (커널은 `apt-mark hold`로 잠겨있어 안 바뀜)
- 단, **파이 재부팅하면 CSI 캡처 설정이 초기화**되므로, 재부팅 후 캡처 전에 반드시 재설정 필요 (문서 10장 참고):
  ```bash
  sudo ifconfig wlan0 up
  sudo nexutil -Iwlan0 -s500 -b -l34 -v$CSI_PARAMS
  sudo iw dev wlan0 interface add mon0 type monitor
  sudo ifconfig mon0 up
  ```

---

## 2026-08-25

### 8. 파이 재가동 + 캡처 환경 점검

- 파이 IP가 `172.30.1.26` → `172.30.1.29`로 바뀜 (DHCP). 앞으로 IP 하드코딩 안 쓰고
  `$PI_HOST`(mDNS)만 쓰도록 문서 3개 전부 수정. 이제 IP 바뀌어도 손댈 것 없음.
- 재부팅 상태라 CSI 설정이 초기화돼 있었음 (`chanspec 0x1007` = 채널 7). 재주입해서 `0xe09b, 149/80` 복구.
- 캡처 실행 순서를 `RUN.md`로 분리해 문서화 (셋업 문서 9·10·13장에 흩어져 있던 것).
- 사전 점검 200패킷 캡처 정상: 드랍 0, **샘플링레이트 71Hz**, 서브캐리어 256개 중 201개 유효.
  → **장비는 멀쩡함.** 호흡(0.33Hz) 대비 샘플링레이트는 200배로 차고 넘침.

### 9. ~~1차 실패 원인 규명~~ → **철회됨** ⚠️

당초 "서브캐리어 히트율이 우연보다 낮으므로 호흡 신호 없음(확정)"이라고 적었으나,
**근거가 된 밴드패스 필터 자체가 고장나 있었음**(아래 11번). 이 항목의 결론은 무효.
가설 A(AGC 공통 게인 제거) 기각은 유효하나, 그 역시 같은 필터를 거쳤으므로 재확인 필요.

### 10. 빈 방 기준선 확보 + 라벨 오류 발견 ⭐

동아리방 인원 6명이 전원 퇴실한 상태에서 캡처 → `data/baseline_empty_20260825.pcap`
(3000패킷, 드랍 0, 22초, 126Hz). **프로젝트 최초로 검증된 빈 방 기준선.**

이 과정에서 **7월 `baseline_empty.pcap`이 실제로는 빈 방이 아니었음**이 밝혀짐 (사용자 확인).
→ 오해 방지를 위해 `baseline_occupied_20260713.pcap` 으로 개명함.
→ 1차 분석은 사람이 있는 방을 "노이즈 플로어"로 놓고 비교한 것. **오독의 근본 원인.**
→ 재발 방지로 `data/README.md` 대장 신설. 새 캡처는 반드시 실제 환경 조건을 기록할 것.

### 11. 🚨 밴드패스 필터가 고장나 있었음 (모든 과거 분석 무효화)

`bandpass_filter`가 전달함수 계수(`b, a`) 형태 + `filtfilt`를 쓰고 있었음.
0.1Hz를 fs로 정규화하면 값이 극단적으로 작아져 배정밀도로 표현이 안 됨:

| fs | 정규화 하한 | 극점 최대반경 | 상태 |
|---|---|---|---|
| 10Hz | 0.02000 | 0.982 | OK |
| 20Hz | 0.01000 | 0.991 | OK |
| **78Hz** | 0.00256 | **1.0039** | **불안정** |
| **126Hz** | 0.00159 | **1.0037** | **불안정** |

극점이 단위원 밖으로 나가 필터가 발산함. 78Hz에서는 출력이 우연히 유한해서
그럴듯한 그래프가 나왔고, 126Hz(오늘 캡처)에서는 `1e8`까지 폭발.

**합성 신호 검증 (fs=126Hz, 0.25Hz 사인파 입력):**
- 기존 `b,a` → 피크 **0.133Hz**(오답), 출력 범위 ±9e37
- SOS → 피크 **0.256Hz**(정답), 출력 범위 ±1.16

→ **수정:** `sp_signal.butter(..., output="sos")` + `sosfiltfilt` 로 교체.
→ 지금까지 관찰된 "밴드 최하단 0.10Hz 쏠림"은 방의 특성이 아니라 **이 버그의 산물**이었음.

### 12. 필터 수정 후 전면 재분석

**호흡 캡처 3개 — 셋 다 16~17 BPM에 수렴:**

| 캡처 | 메트로놈 | 수정 후 결과 |
|---|---|---|
| breath_12bpm | 12 | 0.287Hz = **17.2 BPM** |
| breath_15bpm | 15 | 0.287Hz = **17.2 BPM** |
| breath_20bpm | 20 | 0.271Hz = **16.3 BPM** |

**기준선 2개 (22초로 길이 맞춰 비교):**

| 캡처 | 피크 분포 | 선명도 |
|---|---|---|
| `baseline_empty_20260825` (진짜 빈 방) | 158/166이 밴드 최하단, 나머지 흩어짐 | **2.51** (전체 최저) |
| `baseline_occupied_20260713` (7월, 사람 있었음) | 195/214가 **0.20Hz** | 3.81 |

- 진짜 빈 방은 **선명도가 가장 낮고 대역에 주기 성분이 없음** → 조용한 방의 정상적인 모습.
  최하단 쏠림은 "이 대역에 신호가 없어 가장자리로 흘러내린" 것.
- 7월 파일의 0.20Hz 성분은 **그 방에 있던 사람의 호흡**으로 설명됨 (12 BPM에 해당).

**유력 가설 (미확정):** 시스템은 호흡을 잡고 있었으나 메트로놈 박자가 아니라
**피험자의 자연 호흡수(약 17회/분)**를 잡았다. 17 BPM은 정상 안정 호흡수이고,
사람이 있을 때만 나오고 빈 방에서는 안 나온다는 점이 부합.

**확정 못 하는 이유:** 빈 방은 22초·126Hz 버스트 샘플링, 호흡 캡처는 77초·78Hz라
조건이 달라 직접 비교 불가. 실제로 절대 에너지는 빈 방이 더 높게 나오는데,
버스트 샘플링의 보간 잡음으로 보임.

### 13. 그 외 수정

- `load_amplitude`: tcpdump가 `-c`로 끊길 때 남는 손상 프레임(타임스탬프 0, 전 서브캐리어
  non-finite) 제거. 이게 1개만 섞여도 서브캐리어 256개가 **전멸**해 진단이 실행조차 안 됐음.
- `csi_diagnose.py` 신설: 결합 전에 서브캐리어를 개별로 훑어 "신호가 있는가"를 먼저 판정.
  `--seconds` 로 길이 맞춤 비교 지원.
- `RUN.md`: 캡처를 **패킷 수(`-c`)가 아니라 시간(`timeout 90`) 기준**으로 변경.
  샘플링레이트가 77~127Hz로 변동해 `-c 3000`이 40초가 아닌 22초로 끝난 사례 발생.
- `data/*_plot.png` 전부 재생성 (과거 플롯은 고장난 필터 산물이라 삭제).

### 14. GitHub 저장소 개설

**https://github.com/devsign2026/wifisoloman** (organization `devsign2026`, public)

앞으로 이 저장소로 푸시한다. 원격은 이거 하나만 쓸 것.

공개 저장소이므로 실제 환경 값은 분리했음:
- `config.sh` — 실제 SSID/MAC/호스트명. **`.gitignore` 로 제외, 커밋 금지**
- `config.example.sh` — 템플릿 (공개)
- 문서·명령은 `$PI_HOST` `$TX_MAC` `$CSI_PARAMS` 등 변수를 참조.
  터미널에서 `source config.sh` 후 그대로 복붙 가능.
- `pi_access.md` 의 파이 비밀번호 항목은 삭제, nexutil base64 파라미터도 치환
  (MAC이 인코딩되어 있었음)

`data/baseline_empty.pcap` → `baseline_occupied_20260713.pcap` 개명도 이때 반영.

### 15. ~~재실 여부에 따른 호흡대역 분리 확인~~ → **철회됨** ⚠️ (18번 참조)

6명 재실 상태 90초 대조군 캡처(`baseline_occupied6_20260825.pcap`) 자체는 유효.
그러나 이를 22초 빈 방 캡처와 비교해 "재실 여부가 호흡 대역에서 갈린다"고 한 결론은
7분 빈 방 기준선 확보 후 무너짐. 18번 항목 참조.

### 16. 밴드패스 하한을 0.1 → 0.15Hz 로 변경

90초 캡처에서 서브캐리어 226/243이 밴드 최하단에 몰리는 현상이 있었음.
하한을 0.15Hz로 올리자 같은 캡처가 **0.20Hz(94개) / 0.30Hz(122개)** 로 갈림.

근거는 사후적이 아니라 물리적임 — **0.1Hz = 분당 6회**로, 성인 안정 호흡(12~20회)
아래다. 즉 0.1~0.15Hz 구간에는 호흡이 있을 수 없고 느린 몸 움직임만 들어온다.
그걸 통과시키니 드리프트가 대역을 점령한 것.

**파라미터 조작이 아님을 보증하는 것은 빈 방 대조군이다.** 하한을 올려서 없던 피크가
생기는 것이라면 빈 방에서도 생겨야 하는데, 빈 방은 여전히 최하단에 남는다.

### 17. 서브캐리어 선택 기준 완화

"모든 프레임에서 유한" 조건이라 캡처가 길수록 불리했음. 프레임이 많아질수록
어느 서브캐리어든 -inf 를 한 번쯤 만날 확률이 커져, 100초 캡처에서 유효
서브캐리어가 **83/256** 까지 떨어짐 (22초에서는 166개) → 길이가 다른 캡처끼리 비교가 깨짐.

→ 결측 1% 이하인 서브캐리어는 살리고 드문 -inf 는 선형보간으로 메우도록 변경.
100초 캡처 기준 83 → **243개**로 회복. 이제 길이가 달라도 같은 서브캐리어 집합으로 비교됨.

### 18. 빈 방 7분 기준선 확보 → 15번 결론 철회 🚨

`baseline_empty7min_20260826.pcap` (16349패킷, 드랍 0, 419s, 37Hz).
ping을 `-i 0.05`로 늦춰 41Hz로 수집(기존 142Hz는 호흡 대비 400배 과잉이라 파일만 컸음).

**길이를 39초로 완전히 맞춰 비교:**

| 방 상태 | 우세 피크 | 선명도 |
|---|---|---|
| 0명 | 0.20Hz 152/244 | 2.09 |
| 6명 | 0.20Hz 128/243 | 2.22 |
| 1명 | 0.20Hz 172/242 | 2.99 |

**빈 방에서도 0.20Hz가 똑같이 나옴.** 15번의 분리는 성립하지 않음.

**빈 방 7분을 39초 창으로 쪼갠 결과 (0.20Hz 우세 서브캐리어 수):**
```
115 → 112 → 0 → 74 → 12 → 2 → 1 → 60 → 36 → 6
```
같은 빈 방인데 구간에 따라 "강한 0.20Hz"와 "아무것도 없음"이 모두 나옴.

**원인:** 선명도가 내내 2 수준 = 스펙트럼이 평평함. 이 상태에서 "피크 위치"는
잡음이 FFT 격자 위 어디에 앉았느냐일 뿐이다. 30초 창 분해능이 0.0417Hz인데
관측된 피크들(0.167 / 0.208 / 0.333)이 정확히 그 4·5·8배 지점인 것이 방증.

**방법론적 교훈:** 길이가 다른 캡처를 비교하면 안 되고, 선명도가 잡음 수준일 때
피크 분포를 해석하면 안 된다. 오늘 두 번(9번, 15번) 같은 유형의 오류를 반복했음.

### 19. 선명도 게이트 추가

`csi_diagnose.py` 에 `SHARPNESS_FLOOR = 4.0` 도입. 선명도가 이 아래면
피크 분포를 출력하되 **"해석하지 말 것"을 명시하고 판정을 거부**한다.
15번 유형의 오류를 도구 차원에서 차단.

### 20. 동일 조건 A/B 캡처 — 진폭으로는 검출 실패

`occupied4_20260826.pcap` (16441패킷, 드랍 0, 419s, 37Hz).
빈 방 기준선과 **같은 날·15분 간격·같은 배치·같은 ping·같은 길이·서브캐리어 244개.**

배치: `[맥북] ····· [나][파이] ····· [공유기]`, 맥북 좌측 1m에 1명, 공유기 우측에 2명.
파이가 피험자 바로 앞(1m 이내).

**진폭 기반 30초 창 13개씩:**

| | 선명도 중앙 | 대역 rms |
|---|---|---|
| 빈 방 | 2.11 | 1.954 |
| 사람 4명 | 1.86 | 1.829 |

Mann-Whitney p=0.900 / p=0.779. **구분 안 될 뿐 아니라 방향이 반대.**
장비 1m 안에 4명이 있는데도 빈 방과 구별되지 않음.

### 21. 위상으로 바꾸자 재실 감지 성공 ⭐⭐

같은 데이터를 **CSI 위상**으로 다시 분석. 원시 위상은 CFO/SFO/패킷 검출 지연 때문에
패킷마다 서브캐리어축 선형 램프가 얹혀 있어, 이를 제거(unwrap 후 선형 추세 제거)한 뒤 분석.

**창별 호흡대역(0.15~0.6Hz) 위상 rms:**
```
빈 방   : 0.57 0.67 0.58 0.76 0.61 0.62 0.64 0.56 0.56 0.50 0.47 0.49 0.54
사람4명 : 0.80 0.81 1.27 0.99 1.20 1.15 1.20 0.80 0.92 0.86 1.08 1.23 0.92
```

| | 중앙값 | 범위 |
|---|---|---|
| 빈 방 | 0.569 | 0.47 ~ **0.76** |
| 사람 4명 | 0.989 | **0.80** ~ 1.27 |

**겹침 0/13. Cohen d=3.19. Mann-Whitney p<0.0001.**
임계값 0.78로 분류하면 **26/26 전부 정답.**

→ `csi_presence.py` 신설. `--compare` 로 두 캡처 통계 비교 지원.

**해석의 한계:**
- 검출된 것은 **재실**이지 호흡이 아님. 선명도는 1.86 → 1.93으로 거의 안 올랐으므로
  뚜렷한 주기 성분이 아니라 대역 전체 에너지 증가. 미세한 몸 움직임 포함.
- 사람 4명 기준. 1명은 미확인.
- 임계값 0.78은 이 배치·ping 전용. 과거 캡처(142/78Hz)에 적용하면
  6명 캡처가 0.636, breath_15bpm이 0.723으로 임계값 아래 — 설정이 다르면 전이 안 됨.

### 22. 측정 경로가 공유기→파이가 아니라 맥북→파이임을 확인

`makecsiparams -m` 필터가 맥북 MAC으로 걸려 있어, 파이는 **맥북이 송신한** 프레임에서만
CSI를 뽑는다. 실제 데이터에서 CSI 프레임 3000개 전부 발신 MAC이 `$TX_MAC`(맥북)로 확인됨.

→ **공유기는 ping 목적지일 뿐 측정 경로에 없다.** 사람은 맥북-파이 사이에 있어야 한다.
`nexmon_csi_setup.md` 13장 배치도가 "공유기↔파이 사이에 사람, 맥북은 구석"으로
잘못 그려져 있었음. 7월 실험이 실패한 원인 중 하나일 가능성.

### 23. 실시간 모니터 (csi_live.py + csi_stream.py)

**1차 시도(청크 방식)는 실패.** 5초씩 끊어 캡처하고 SSH로 파일을 통째로 받는 구조라
지연이 심했고, 같은 원격 경로(`/tmp/csi_live_chunk.pcap`)를 쓰는 인스턴스가 둘 이상
돌면 서로 파일을 덮어써 버퍼가 안 차는 버그도 있었음.

**2차(스트리밍)로 교체.** `tcpdump -U -w -` (패킷마다 flush) 출력을 SSH 파이프로
받아 프레임 도착 즉시 처리:
- 13초 후 파형 표시, 24초 후 재실 판정, 이후 초당 2회 갱신
- 대역 에너지는 30초 창이 필요 (호흡 0.2Hz보다 짧으면 무의미) — 이건 물리적 하한

`csi_stream.py` 신설. CSIKit은 파일 단위로만 읽어 실시간에 못 쓰므로 직접 파싱:
```
pcap 글로벌 헤더 24B
레코드마다 ts_sec(4) ts_usec(4) incl_len(4) orig_len(4) + 패킷
패킷에서 60B(Ethernet14+IP20+UDP8+nexmon18) 건너뛰고 int16 쌍(실수,허수)
```

**검증:** 같은 파일에서 첫 프레임들의 타임스탬프·CSI 값이 CSIKit과 완전 일치.
프레임 수는 직접 파서 16349개(tcpdump 캡처 수와 동일) vs CSIKit 15507개로,
**CSIKit이 842개를 버림**(패킷은 전부 magic `1111`, chip `6500`으로 정상).
end-to-end로도 재실 분리가 재현됨: 빈방 0.519~0.733 / 사람 0.709~1.215, p<0.0001.
겹침이 1창 생겨 임계값은 0.75로 조정.

### 24. 무호흡 판별 시도 — 실패 🚨

배치를 논문 조건에 맞춰 변경 (맥북↔파이 **1m**, 사람이 정중앙, 가슴 높이, 혼자, 냉방 OFF).
7분 자기 라벨링 캡처 `apnea_test_20260826.pcap` (16722패킷, 드랍 0, 419s, 38Hz):
0~2분 정상 / **2:00~2:30 숨참기** / 2:30~4:30 정상 / **4:30~5:00 숨참기** / 5~7분 정상.
이어서 퇴실 후 `empty_after_20260826.pcap` (3분).

**대역 에너지 (10초 창):** 정상 0.766 vs 숨참기 0.642, p=0.038.
약하지만 유의. 그러나 **주파수 증거가 전혀 없음**:

| 구간 (28초로 통일) | 합산 피크 | 선명도 |
|---|---|---|
| 정상 30~58s | 0.291Hz | 1.79 |
| 정상 60~88s | **0.416Hz** | 2.18 |
| 정상 90~118s | **0.167Hz** | 2.05 |
| 정상 180~208s | **0.541Hz** | 1.89 |
| 숨참기1 | 0.167Hz | 1.97 |
| 숨참기2 | 0.130Hz | 2.24 |

정상 호흡 구간의 피크가 0.167~0.541Hz로 제멋대로 튄다. 진짜 호흡이면 피험자의
실제 호흡수 한 곳에 일관되게 서야 한다. 선명도도 정상·숨참기 모두 2 근처로 평평.
→ 대역 에너지 차이는 호흡이 아니라 **숨 참을 때 몸이 굳어 잔움직임이 준 효과**로 보임.

**3상태 비교 (10초 창):**

| 상태 | 중앙값 | 범위 | n |
|---|---|---|---|
| 호흡 중 | 0.766 | 0.445~1.147 | 70 |
| 숨 참기 | 0.642 | 0.394~1.030 | 12 |
| **빈 방** | **1.215** | 0.656~1.610 | 35 |

- 호흡 vs 숨참기: **12창 중 10창 겹침.** 분류기로 못 씀
- **빈 방이 사람 있을 때보다 높음 (방향 반대).** 배치를 1m로 바꾸면서
  기존 임계값(0.75)이 무효가 됨 → **재실 감지조차 이 배치에서는 작동 안 함**

원인 미확정: 1m 근접 배치에서 인체가 멀티패스를 막아 채널을 오히려 안정화시켰을
가능성, 또는 퇴실 직후 캡처라 문 근처 움직임이 섞였을 가능성. 검증 안 됨.

### 25. 결론 — 이 하드웨어의 경계

**되는 것:** 재실 감지 (단, 배치마다 재보정 필요). 2026-08-26 기존 배치에서
겹침 0, p<0.0001, Cohen d=3.19 로 검증.

**안 되는 것:** 호흡수 추출, 무호흡 판별.

**원인 — 수신 안테나 1개.** Pi 4B의 BCM43455c0은 1x1이다 (CSI shape `(256,1,1)`,
core 0 하나만 관측됨). 참고한 연구들의 핵심 기법이 전부 다중 안테나를 요구한다:
FarSense는 두 안테나의 CSI **비율**, PhaseBeat는 안테나 **간 위상차**로 공통 위상
오차를 제거한다. 호흡 신호가 워낙 미약해 그 처리 없이는 드러나지 않는다.
소프트웨어로 우회 가능한 제약이 아니다.

⚠️ **안전 용도 금지.** 호흡 정지 감지에 쓸 수 없다. 호흡이 멈춘 사람도 여전히
"재실"로 잡히므로 정상으로 표시된다. 호흡 중/숨참기 구분은 12창 중 10창이 겹친다.

### 26. 구석 이설 + 조건 최적화

전체 장비를 동아리방 반대편으로 이설. 이설 과정 기록:
- 파이 랜선을 뽑고 옮기는 동안 접속 상실 → 랜선 재연결로 복구 (mDNS 정상 동작)
- 재부팅으로 CSI 설정 초기화 (`chanspec 0x1007, 7`) → 재주입 (`0xe09b, 149/80`)
- 이설 직후 RSSI -59dBm 로 악화 → 맥북-파이 간격을 **1m** 로 조정하자 **-43dBm** 로 개선
  (이전 배치 -51dBm 보다도 8dB 강함. 역대 최강)
- ping 을 `-i 0.02` 로 상향 → 샘플링 **42Hz** (기존 19Hz의 2배)

**이설로 기존 기준선·임계값은 전부 무효화됨.**

### 27. 최상 조건에서의 재측정 — 정지한 사람은 검출 불가 🚨

`empty_corner_20260826.pcap` (빈 방 7분) / `person_corner_20260826.pcap` (사람 7분).
같은 자리·같은 설정·연속 촬영.

| | 중앙값 | 범위 | n |
|---|---|---|---|
| 빈 방 | 0.579 | 0.471~0.721 | 13 |
| 사람 (정지, 3분 이후) | 0.595 | 0.566~0.633 | 6 |

**겹침 6/6, p=0.26, Cohen d=0.34 — 구분되지 않음.**

그러나 같은 캡처의 앞부분(입실 동작 구간)은 뚜렷하게 높다:
```
120s 0.905  ← 걸어 들어오는 중
150s 0.862  ← 자리 잡는 중
180s 0.621  ← 앉아서 정지
210s 0.566  ← 정지 (빈 방 0.579 와 동일)
```

→ **지금까지 "재실 감지"라고 부른 것은 실제로는 "움직임 감지"였다.**
2026-08-26 오전에 검증했던 캡처는 4~6명이 자연스럽게 움직이던 상황이라 갈렸던 것.
**가만히 있는 사람 한 명은 빈 방과 구별되지 않는다.**

**스펙트럼 대조 (결정적):** 빈 방에서 0.333Hz(20 BPM) 피크가 히트율 **87%** 로 나온다.
사람이 앉아 있을 때(44%)보다 오히려 "호흡처럼" 보인다. 선명도는 양쪽 다 2.2~2.5로 평평.
→ 대조군이 없었다면 "20 BPM 호흡 검출"로 오독했을 것.

### 28. 출입 이벤트 실험 — 이벤트는 검출, 방향은 불가

`inout_20260826.pcap`. 입실 3회 / 퇴실 3회를 정해진 시각에 반복.
움직임 지표는 0.3~8Hz 대역 위상 변동(걷기는 호흡보다 훨씬 빠르다).

**주기성 확인:** 자기상관이 30초 배수에서만 양(+0.740/+0.628/+0.545/+0.422),
그 사이는 전부 음. 프로토콜 리듬이 신호에 남아 있음.

**패턴 정렬:** 최적 오프셋 +61초 (제가 준 40초 지연 + 퇴실 시간과 물리적으로 일치).
방 안 2.035 vs 방 밖 1.735, p=0.0153.
순열검정 2000회 (귀무 95분위 +0.244 vs 실제 +0.300) → **p=0.0035**.

**그러나 분해해 보면:**

| 검정 | 결과 | 판정 |
|---|---|---|
| 전환 순간(±5초) vs 나머지 | 2.040 vs 1.793, p=0.039 | ✅ 검출 |
| 순수 착석 vs 순수 부재 (전환 제외) | 1.957 vs 1.705, **p=0.406** | ❌ |
| 방향: 입실 vs 퇴실 (±6초) | 2.048 vs 1.992, **p=0.468** | ❌ |

전체 효과는 **전부 전환 순간에서 나온 것**이며, 착석 자체는 27번과 동일하게 검출되지 않는다.
방향 판별도 불가 — 현재 지표는 "얼마나 움직였나"만 재므로 방향 정보가 없다.
방향을 보려면 위상 변화의 부호가 필요한데 안테나 1개로는 안정적으로 뽑기 어렵다.

⚠️ 이벤트가 6회뿐이고 효과크기도 14%로 작다. 발표에 쓰려면 20~30회로 늘려 재확인할 것.

### 29. 의도적 깊은 호흡 실험 — 호흡 검출 불가 확정 🔒

구석 세션에서 유일하게 안 해본 조합("최상 조건 + 라벨된 숨참기")을 채움.
`deepbreath_20260826.pcap` (386s, 36.8Hz, RSSI -43dBm).

자연 호흡이 아니라 **의도적으로 과장한 깊은 호흡**을 일정 리듬으로 수행:
2초 들이쉬고 2초 내쉬기 = **0.25Hz(15 BPM)**, 가슴을 크게 부풀림.
0~2분 깊은호흡 / 2:00~2:30 숨참기 / 2:30~4:30 깊은호흡 / 4:30~5:00 숨참기 / 5~7분 깊은호흡.

**목표 주파수에 아무것도 없음:**

| | 0.25Hz(±0.02) 대역 에너지 비중 |
|---|---|
| 깊은 호흡 | 8.6% |
| 빈 방 (같은 자리) | **9.6%** |
| | p=0.80 |

빈 방이 오히려 더 높다. 의도적으로 만든 0.25Hz 진동이 신호에 전혀 반영되지 않았다.
구간별 피크도 0.143 / 0.428 / 0.122 / 0.571 / 0.224 / 0.235Hz 로 제멋대로 튀고
선명도는 1.96~2.70 으로 전부 평평.

**숨참기도 구분 불가:**
```
깊은 호흡  0.617
숨 참기    0.647   ← 오히려 높음
빈 방      0.566
```
`깊은호흡 > 숨참기` p=0.74 로 방향조차 반대.

`깊은호흡 > 빈방` 은 p=0.0127 로 유의하나 Cohen d=0.29 로 효과가 미미하고
0.25Hz 성분과 무관하다. 호흡 주파수가 아니라 **깊게 숨 쉬느라 생긴 미세한 몸 움직임**을
잡은 것으로 보인다.

**의의:** "자연 호흡이 너무 약해서 안 잡힌 것 아니냐"는 반론을 닫는 데이터다.
과장된 호흡을 일정 주파수로 만들어 줘도 검출되지 않으므로,
**이 하드웨어로 호흡 감지는 불가능하다**가 확정된다.

---

## 다음에 할 일 (갱신 2026-08-26)

0. ~~빈 방 기준선 재캡처~~ → **완료** (18번, 7분)
1. ~~사람 있는 캡처~~ → **완료** (20~21번). 위상으로 재실 감지 성공.
2. ~~호흡수 추출~~ → **이 하드웨어로는 불가 판정** (24~25번)
3. **재실 감지의 배치 의존성 규명.** 1m 배치에서 빈 방이 더 높게 나온 원인이
   근접 배치의 물리 효과인지 퇴실 직후 오염인지 확인 필요. 같은 배치에서
   충분한 시간을 두고 빈 방을 다시 찍어볼 것.
4. **호흡까지 하려면 하드웨어 교체.** Intel 5300 계열(수신 안테나 2~3개) 2대.
   현 Pi 구성으로는 진행 불가.
2. 위가 성립하면 그때 메트로놈 실험으로 넘어갈 것. 순서를 뒤집었던 게 실책이었음
   — "박자 추종"은 "감지 자체"보다 어려운 과제인데 먼저 시도했음.
3. 캡처는 반드시 `timeout 90`(시간 기준), 조건은 `data/README.md`에 기록.
4. 분석은 `csi_diagnose.py` 먼저 → ✅ 나올 때만 `csi_pipeline.py`.
