# 하드웨어 계획

> 현재 구성의 한계와, 확정된 교체 계획(ESP32-C5)을 정리한다.
> 실험 결과의 근거는 [history.md](./history.md), 결론 요약은 [README.md](./README.md).

---

## 현재 구성

| 역할 | 장비 | 문제 |
|---|---|---|
| CSI 수신 | Raspberry Pi 4B (BCM43455c0) | **수신 안테나 1개** ← 근본 한계 |
| 트래픽 발생 | 남는 맥북 (`ping -i 0.02`) | 크고, 수동 조작 필요 |
| 신호원 | ipTIME 5GHz, 채널 149/80MHz | — |

---

## 계획 ① — 트래픽 발생기를 ESP32-C5 로 교체 (확정)

### 왜

맥북은 ping을 켜고 끄려면 사람이 직접 터미널을 만져야 한다. 실제로 실험 중
"ping 껐다 켜면 패킷 0개가 되는 현상" 때문에 여러 번 막혔다. ESP32-C5는
파이가 네트워크로 제어하거나 전원만 넣으면 자동으로 동작하게 만들 수 있다.

| | 맥북 (현재) | ESP32-C5 |
|---|---|---|
| 크기 | 노트북 한 대 | 엄지 크기 |
| ping 제어 | 사람이 터미널 입력 | 부팅 시 자동 / 원격 제어 |
| 프라이빗 MAC 문제 | 있음 (매번 확인 필요) | 없음 (MAC 고정) |
| 전원 | 배터리 관리 필요 | USB 상시 급전 |
| 배치 자유도 | 낮음 | 높음 |

### 왜 하필 C5 인가

**ESP32 계열 중 5GHz를 지원하는 것은 C5 뿐이다.**

| 모델 | 대역 | 채널 149 |
|---|---|---|
| ESP32 / S2 / S3 / C3 / C6 | 2.4GHz 전용 | ❌ |
| **ESP32-C5** | 2.4 + 5GHz 듀얼밴드 | ✅ |

2.4GHz로 내려가면 서브캐리어가 **256개 → 64개**(20MHz)로 줄고 간섭도 심해진다.
지금도 감도가 부족하므로 그 방향은 손해다.

### 구매 전 반드시 확인할 것

- [ ] **채널 149 사용 가능 여부** — 국가/지역 설정에 따라 5GHz 상위 채널이 막힐 수 있다
- [ ] **SDK(ESP-IDF)의 5GHz STA 지원 성숙도** — 비교적 최근 칩이라 확인 필요
- [ ] 안테나 형태 (온보드 PCB 안테나 / 외부 안테나 커넥터). **외부 안테나 쪽이 유리**
- [ ] 국내 유통 및 재고

> ⚠️ 이 문서 작성 시점(2026-08)에 C5의 수급·지원 상태를 직접 확인하지 않았다.
> 구매 전 데이터시트와 ESP-IDF 문서를 직접 볼 것.

### 교체할 때 바꿔야 하는 것 (놓치기 쉬움)

**1. MAC 필터 재생성** — CSI는 지정한 MAC이 보낸 패킷에서만 추출된다.
```bash
makecsiparams -c 149/80 -C 1 -N 1 -m <ESP32-C5의 MAC>
```
출력값을 `config.sh` 의 `CSI_PARAMS` 에 넣는다. `TX_MAC` 도 함께 갱신.

**2. 배치 재설계** — 측정 경로는 **송신기 → 파이** 다.
```
[ESP32-C5] ←── 1~1.5m ──→ [파이]
       └──── 사이에 사람 ────┘

[공유기] ← ping 목적지. 위치 무관
```
사람은 **ESP32와 파이 사이**에 앉는다. 공유기가 아니다.
(이 혼동으로 7월 실험 전체를 날린 적이 있다 — history.md 22번)

**3. 파이 바로 옆에 두지 말 것** — 송신기가 수신기와 수 cm 거리면 근접 전파가
수신기 AGC를 포화시켜 CSI가 오염된다. 최소 1m 이상 띄운다.

**4. 패킷 레이트 확보** — 초당 20~50패킷이면 충분하다(실측 19~42Hz).
ping이든 UDP 연속 송신이든 무방하다.

**5. 기준선 전면 재캡처** — 송신기가 바뀌면 채널 특성이 완전히 달라진다.
기존 빈 방 기준선과 임계값은 **전부 무효**다. 빈 방부터 다시 찍을 것.

---

## 계획 ② — 안테나 개수 (미해결, 근본 문제)

**ESP32-C5로 바꿔도 호흡 감지는 여전히 불가능하다.** 송신기를 교체하는 것이지
수신기를 바꾸는 게 아니기 때문이다. 파이의 수신 안테나는 1개 그대로다.

실측으로 확인된 한계 (2026-08-26, 최상 조건):

| 대상 | 결과 |
|---|---|
| 움직임 이벤트 | ✅ 검출 |
| 정지한 사람의 재실 | ❌ |
| 이동 방향 | ❌ |
| 호흡 (자연 / 의도적 과장) | ❌ |

참고 연구들(FarSense, PhaseBeat 등)의 핵심 기법은 **안테나 간 CSI 비율·위상차**로
공통 위상 오차를 제거하는 것이다. 안테나가 1개면 원리적으로 쓸 수 없다.

### 다중 안테나 CSI 선택지

| 장비 | 수신 안테나 | 비고 |
|---|---|---|
| **Intel 5300** (PCIe/mPCIe) | 3 | 참고 논문 대부분이 사용. 구형이라 중고 수급 필요. Linux CSI Tool + 수정 드라이버 |
| Atheros ath9k 계열 | 2~3 | Atheros CSI Tool. 지원 칩 제한적 |
| ESP32 계열 | 1 | CSI API는 있으나 안테나 1개. 해결책이 아님 |

> ⚠️ **ESP32-C5를 두 대 놓아도 안테나 배열이 되지 않는다.**
> 독립된 장치는 클럭이 서로 다르므로, 논문들이 쓰는 코히런트 위상차 처리가 불가능하다.
> 안테나 배열은 **하나의 수신기에 여러 안테나**가 붙어 있어야 성립한다.

---

## 정리

- **ESP32-C5 교체** → 실험 편의성 개선. 권장.
- **호흡 감지** → 수신기를 다중 안테나 장비로 바꾸지 않는 한 불가능.
- 두 가지는 별개의 문제다. 전자를 해도 후자는 해결되지 않는다.
